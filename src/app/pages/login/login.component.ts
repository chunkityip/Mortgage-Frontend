import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  template: `
    <div class="login">
      <h1>Login</h1>
      <p>Login screen placeholder. Use the RoleSwitcher in the header to sign in as a test user.</p>
    </div>
  `,
  styles: [`.login { padding: 24px; }`],
})
export class LoginComponent {}
